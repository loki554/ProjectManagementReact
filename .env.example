DB_PASSWORD=changeme

# MinIO из docker-compose. Нужны, только если переключаете хранилище файлов на S3
# (app.storage.type=s3 в application-dev.yml); по умолчанию файлы лежат на диске.
MINIO_ROOT_USER=pmtracker
MINIO_ROOT_PASSWORD=changeme-minio
