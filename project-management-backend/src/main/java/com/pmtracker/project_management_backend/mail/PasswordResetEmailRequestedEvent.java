package com.pmtracker.project_management_backend.mail;

/**
 * «Ссылку на сброс пароля нужно отправить вот на этот адрес». Публикуется внутри транзакции,
 * создавшей токен, обрабатывается после её коммита — см. {@link MailDispatcher}.
 * <p>
 * Здесь единственное место, где сырое значение токена живёт после генерации: в БД лежит только
 * его SHA-256, поэтому в письмо его надо донести именно так. По той же причине событие не стоит
 * логировать целиком.
 */
public record PasswordResetEmailRequestedEvent(String email, String token) {
}
