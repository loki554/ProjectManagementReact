package com.pmtracker.project_management_backend.export;

/**
 * Готовая выгрузка: имя файла и его содержимое (4.12).
 *
 * <p>Имя собирает сервис, а не контроллер, и по той же причине, что у отчёта по времени
 * (TimeReportCsv): в нём слаг проекта и дата снятия — ровно то, что отличает две выгрузки
 * друг от друга в папке «Загрузки», — а контроллер про дату не знает и знать не должен.
 *
 * <p>Обобщённая по содержимому: CSV и Markdown уезжают строкой, JSON — объектом, который
 * сериализует Jackson. Заводить под это две почти одинаковые записи значило бы разложить
 * одно решение об именах файлов по двум местам.
 */
public record ExportFile<T>(String filename, T content) {
}
