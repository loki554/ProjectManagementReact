package com.pmtracker.project_management_backend.storage;

import java.time.Instant;

/**
 * Файл, лежащий в хранилище, как его видит сверка с базой (3.6): путь, по которому на него
 * ссылались бы строки БД, и время последней записи.
 *
 * <p>Время нужно не ради отчётности: только что записанный файл вполне может ещё не иметь
 * строки в БД — транзакция, которая её создаёт, коммитится позже. Без возрастного порога
 * уборка удаляла бы такие файлы прямо из-под живой загрузки.
 */
public record StoredObject(String relativePath, Instant lastModifiedAt) {
}
