package com.pmtracker.project_management_backend.report;

/**
 * Готовая CSV-выгрузка: имя файла и его содержимое.
 *
 * <p>Имя собирает сервис, а не контроллер, и это не мелочь: в нём слаг проекта и границы
 * периода, то есть ровно то, что отличает две выгрузки друг от друга в папке «Загрузки».
 * Контроллер о периоде уже не знает — он мог не получить его вовсе (границы по умолчанию
 * выбирает сервис).
 */
public record TimeReportCsv(String filename, String content) {
}
