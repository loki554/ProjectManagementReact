package com.pmtracker.project_management_backend.task.dto;

/**
 * Итог массовой правки (4.6).
 *
 * <p>Отдаём число, а не сами задачи: клиент после успеха всё равно инвалидирует список
 * целиком (изменившиеся поля входят в фильтры и сортировку, так что состав и порядок
 * страницы после правки другой), и тащить ради этого до двухсот полных TaskResponse —
 * мегабайты, которые тут же выбрасываются.
 *
 * @param updated сколько задач реально изменилось. Не то же самое, что размер выделения:
 *                задача, у которой запрошенное значение уже стоит, не меняется и не
 *                попадает ни в ленту активности, ни в этот счётчик. «Обновлено: 3 из 5» —
 *                честный ответ на «перевести пять задач в DONE, две из которых уже DONE».
 */
public record BulkUpdateTasksResponse(int updated) {
}
