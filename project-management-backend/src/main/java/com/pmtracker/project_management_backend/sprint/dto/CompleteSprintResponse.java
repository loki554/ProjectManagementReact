package com.pmtracker.project_management_backend.sprint.dto;

/**
 * Результат завершения спринта: сам спринт и сколько незакрытых задач при этом переехало.
 *
 * <p>Число здесь не для красоты: «завершено, перенесено 7 задач» — единственное место, где
 * человек видит цену закрытия до того, как пойдёт искать эти задачи в следующем спринте.
 * Отдавать вместо счётчика их список незачем — он уже открывается фильтром по спринту, в
 * который они переехали.
 */
public record CompleteSprintResponse(
        SprintResponse sprint,
        int movedTasks
) {
}
