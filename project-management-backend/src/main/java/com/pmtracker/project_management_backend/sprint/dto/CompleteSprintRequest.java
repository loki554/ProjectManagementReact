package com.pmtracker.project_management_backend.sprint.dto;

import java.util.UUID;

/**
 * Завершение спринта (4.9): что делать с задачами, которые не успели закрыть.
 *
 * <p>Вопрос обязателен по смыслу, но не по схеме: {@code null} — это законный ответ
 * «в бэклог», и он же ответ по умолчанию. Оставить недоделанные задачи в закрытом спринте
 * нельзя ни при каком ответе — иначе прогресс завершённого спринта навсегда останется
 * неполным, а сами задачи потеряются в архиве, из которого их никто не перечитывает.
 *
 * @param moveUnfinishedToSprintId куда переложить незакрытые задачи: id другого незакрытого
 *                                 спринта того же проекта либо {@code null} — в бэклог
 */
public record CompleteSprintRequest(
        UUID moveUnfinishedToSprintId
) {
}
