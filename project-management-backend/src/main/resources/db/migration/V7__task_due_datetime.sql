-- Срок выполнения задачи становится датой-временем (не только датой) — нужно для
-- обратного отсчёта по часам в последние сутки перед дедлайном (см. фронтенд).
-- Существующие значения (полночь по UTC) переносятся как есть.
ALTER TABLE tasks
    ALTER COLUMN due_date TYPE TIMESTAMPTZ USING due_date::timestamptz;
