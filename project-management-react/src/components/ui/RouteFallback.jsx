/**
 * Что видно, пока едет файл страницы (5.4).
 *
 * Намеренно не текст «Загрузка…» и не спиннер: у страниц этого приложения есть общая
 * форма — заголовок, под ним содержимое, — и три серые полосы на её месте читаются как
 * «сейчас будет страница», а не как «что-то происходит». Текста нет и по второй причине:
 * локаль к этому моменту уже загружена, но переводить нечего — надпись всё равно
 * мелькнёт на доли секунды, а лишний ключ в трёх файлах останется навсегда.
 *
 * Полноценные скелетоны списков и карточек — отдельный пункт (5.8); здесь только заглушка
 * маршрута, и подменять ею тот пункт не нужно.
 */
export function RouteFallback() {
  return (
    <div role="status" aria-busy="true" className="mx-auto max-w-3xl animate-pulse px-4 py-10">
      <span className="sr-only">…</span>
      <div className="h-6 w-48 rounded bg-gray-200 dark:bg-gray-700" />
      <div className="mt-6 h-24 rounded bg-gray-100 dark:bg-gray-800" />
      <div className="mt-3 h-24 rounded bg-gray-100 dark:bg-gray-800" />
    </div>
  )
}
